<nav class="navbar navbar-expand-lg" style="background: #252525;">
  <div class="container-fluid">
    <%= link_to "Home", root_path, class: "navbar-brand text-light" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <!-- Left-side Navigation -->
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= link_to "New Product", new_product_path, class: "nav-link text-light" %>
        </li>
      </ul>

      <!-- Account Section -->
      <div class="d-flex align-items-center">
        <div class="me-3 text-light">
          <% if authenticated? %>
            <span><%= button_to "Đăng xuất", session_path, method: :delete, class: "btn btn-outline-light btn-sm" %></span>
          <% else %>
            <span><%= link_to "Đăng nhập", new_session_path, class: "btn btn-outline-light btn-sm" %></span>
          <% end %>
        </div>

        <!-- Search Form -->
        <form class="d-flex" role="search">
          <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-light" type="submit">Search</button>
        </form>
      </div>
    </div>
  </div>
</nav>
